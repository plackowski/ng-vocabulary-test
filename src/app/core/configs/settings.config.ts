import { CATEGORY } from '@app/model/category';
import { LEVEL } from '@app/model/level';
import { MODE } from '@app/model/mode';

export const wordsPerPage = 5;
export const defaulMode = MODE.LANG_PL;
export const defaultCategory = CATEGORY.ALL;
export const defaultLevel = LEVEL.ALL;
